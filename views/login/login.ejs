<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background: linear-gradient(135deg, #667eea, #764ba2, #fb8938, #f95a2e);
      background-size: 400% 400%;
      animation: gradientBG 50s ease infinite;
    }

    @keyframes gradientBG {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .glass {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .input-group {
      position: relative;
    }

    .input-label {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      transition: all 0.5s ease-in-out;
      pointer-events: none;
      color: #666;
    }

    input:focus + .input-label,
    input:not(:placeholder-shown) + .input-label {
      top: 0.2rem;
      left: 0.75rem;
      font-size: 0.75rem;
      color: #303133;
      background: rgba(255,255,255,0.8);
      padding: 0 0.25rem;
      border-radius: 0.25rem;
    }
  </style>
</head>
<body class="flex items-center justify-center min-h-screen text-white">
  <div class="w-full max-w-md p-8 space-y-6 rounded-2xl shadow-2xl glass">
    <h2 class="text-3xl font-bold text-center">Welcome Back</h2>
    <form action="/login" method="POST" class="space-y-5">
      
      <div class="input-group">
        <input type="email" name="email" id="email" placeholder=" " required
          class="w-full px-4 pt-5 pb-2 bg-white bg-opacity-80 text-black rounded-xl focus:ring-2 focus:ring-blue-400 focus:outline-none shadow-sm"/>
        <label for="email" class="input-label">Email address</label>
      </div>

      <div class="input-group">
        <input type="password" name="password" id="password" placeholder=" " required
          class="w-full px-4 pt-5 pb-2 bg-white bg-opacity-80 text-black rounded-xl focus:ring-2 focus:ring-blue-400 focus:outline-none shadow-sm"/>
        <label for="password" class="input-label">Password</label>
      </div>

      <div class="flex items-center justify-between text-sm">
        <label class="flex items-center space-x-2">
          <input type="checkbox" class="rounded text-blue-500 border-gray-300" />
          <span>Remember me</span>
        </label>
        <a href="#" class="hover:underline">Forgot password?</a>
      </div>

      <button type="submit"
        class="w-full py-3 font-semibold bg-gradient-to-r from-pink-500 to-yellow-500 text-white rounded-xl hover:opacity-90 transition duration-300 shadow-lg">
        Login
      </button>
    </form>
    <p class="text-sm text-center">Don’t have an account? 
      <a href="/register" class="text-blue-300 hover:underline">Sign up</a>
    </p>
  </div>
</body>
</html>
